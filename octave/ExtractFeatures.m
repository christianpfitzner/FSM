%% input parameter: 
%        W: input matrix containing points for feature extraction
%% output parameter: 
%          f: output vector containing features
function [f] = maxmin(W )
   if(a >= b )
        max = a;
        min = b;
        return;
   else
        max = b;
        min = a;
        return;
   endif
endfunction